spring:
  application:
    name: dailyApi
  config:
    import: properties.yml
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      url: jdbc:mysql://localhost:3306/ai_user?timezone=UTC
      username: root
      password: 050109

mybatis:
  mapper-locations: classpath:mapper/*.xml

ai-chat:
  ais:
    deepseek:
      name: ${ai.deepseek.name}
      url: ${ai.deepseek.url}
      model: ${ai.deepseek.model}
      key: ${ai.deepseek.key}
    kimi:
      name: ${ai.kimi.name}
      url: ${ai.kimi.url}
      model: ${ai.kimi.model}
      key: ${ai.kimi.key}

meitu:
  meitus:
    cutout:
      key: ${meitu.meitu_cutout.key}
      secret: ${meitu.meitu_cutout.secret}
    common:
      key: ${meitu.meitu_common.key}
      secret: ${meitu.meitu_common.secret}
jwt:
  secret: xWs93kr2Wp0FwnkWIisEJY5Ebqi2avn897G/Pm/rLvu/lWFSfANusbiAajCpEQblkJ7uqGLpbkEGMnFtZNThyg==
  expiration: 86400